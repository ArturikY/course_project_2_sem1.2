<?php
/**
 * Пример конфигурационного файла
 * Скопируйте в config.php и измените под свои настройки
 */

return [
    'database' => [
        'host' => 'localhost',
        'dbname' => 'dtp_analysis',
        'user' => 'root',
        'pass' => '', // Измените если нужно
        'charset' => 'utf8mb4'
    ],
    
    'import' => [
        'batch_size' => 1000, // Размер батча для импорта
        'ndjson_file' => __DIR__ . '/moskva.ndjson'
    ],
    
    'api' => [
        'default_period_days' => 30, // Период по умолчанию для фильтрации
        'max_bbox_size' => 0.1, // Максимальный размер bbox (градусы)
        'grid_size_meters' => 250 // Размер ячейки сетки в метрах
    ]
];

