# 🔧 Решение проблемы с построением маршрутов

## Ошибки, которые вы видите:

```
NS_BINDING_ABORTED
OpaqueResponseBlocking
Загрузка <script> по адресу «https://api-maps.yandex.ru/services/search/v2/...» не удалась
```

## Причины:

1. **Домен не добавлен в разрешенные** - самая частая причина
2. **Сервис маршрутизации не включен** в кабинете разработчика
3. **CORS блокировка** браузером

## ✅ Решение (пошагово):

### Шаг 1: Настройка домена в кабинете разработчика

1. Откройте https://developer.tech.yandex.ru/
2. Выберите ваш проект
3. Найдите раздел **"Настройки проекта"** или **"Ограничения"**
4. В разделе **"Разрешенные домены"** добавьте:
   - `localhost`
   - `localhost:8000`
   - `127.0.0.1`
   - `127.0.0.1:8000`
5. **Сохраните изменения**

### Шаг 2: Проверка сервисов

Убедитесь, что включены:
- ✅ **JavaScript API и HTTP Геокодер**
- ✅ **Маршрутизация** (Router API)
- ✅ **Геокодер**

### Шаг 3: Проверка API ключа

1. Убедитесь, что в `index.html` строка 428 содержит ваш реальный API ключ:
   ```html
   <script src="https://api-maps.yandex.ru/2.1/?apikey=ВАШ_РЕАЛЬНЫЙ_КЛЮЧ&lang=ru_RU"></script>
   ```
2. Ключ должен быть активирован (активация занимает до 15 минут)

### Шаг 4: Очистка кэша браузера

1. Откройте консоль браузера (F12)
2. Нажмите правой кнопкой на кнопку обновления
3. Выберите **"Очистить кэш и жесткая перезагрузка"**

### Шаг 5: Проверка работы

После всех настроек:
1. Обновите страницу
2. Откройте консоль (F12)
3. Попробуйте построить маршрут
4. Если ошибки остались - пришлите новые сообщения из консоли

## 🔄 Альтернативное решение

Если `routePanelControl` не работает, код теперь использует **MultiRoute API** напрямую через функцию `buildRoute()`. Это более надежный вариант.

## 📝 Что изменилось в коде:

1. ✅ Добавлена обработка ошибок для `routePanelControl`
2. ✅ Функция `buildRoute()` теперь использует `MultiRoute API` напрямую
3. ✅ Добавлены обработчики успеха и ошибок
4. ✅ Улучшена визуализация маршрута

## ⚠️ Важно:

- **Домен обязательно должен быть добавлен** в разрешенные домены
- Без этого маршрутизация не будет работать даже с правильным API ключом
- Изменения в кабинете могут применяться с задержкой до 15 минут
