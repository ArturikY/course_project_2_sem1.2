# 🛣️ Анализ опасности маршрутов

## 💡 Идея

Пользователь строит маршрут между двумя точками через Yandex.Maps. Yandex предоставляет 3 варианта маршрута. Нужно:
1. Получить доступ к координатам всех 3 маршрутов
2. Проверить, через сколько опасных зон проходит каждый маршрут
3. Оценить опасность каждого маршрута
4. Показать пользователю сравнение и рекомендацию

---

## ✅ Можно ли получить доступ к маршрутам?

**Да, можно!** Yandex.Maps API позволяет получить координаты маршрута.

### Как это работает:

1. **Yandex.Maps строит маршрут** через `ymaps.multiRouter.MultiRoute` или `routePanelControl`
2. **Можно получить геометрию маршрута** (линию) через события API
3. **Для каждого из 3 вариантов маршрута** доступны координаты точек

---

## 📍 Как получить координаты маршрута?

### Вариант 1: Через routePanelControl

```javascript
// Получение маршрута из панели маршрутов
const routePanel = map.controls.get('routePanelControl');

routePanel.routePanel.getRouteAsync().then(function(route) {
    const geometry = route.getGeometry(); // Получаем линию маршрута
    const coordinates = geometry.getCoordinates(); // Массив координат [lat, lon]
    
    // Теперь у нас есть все точки маршрута
    // Можно проверить пересечение с опасными зонами
    analyzeRouteSafety(coordinates);
});
```

### Вариант 2: Через MultiRoute

```javascript
// Получение маршрутов из MultiRoute
routeMultiRoute.events.add('update', function() {
    const routes = routeMultiRoute.getRoutes();
    
    routes.each(function(route, index) {
        const geometry = route.getGeometry(); // Получаем линию маршрута
        const coordinates = geometry.getCoordinates(); // Массив координат
        
        // Анализируем каждый маршрут
        const safety = analyzeRouteSafety(coordinates, index);
        console.log(`Маршрут ${index + 1}:`, safety);
    });
});
```

### Структура данных:

```javascript
// coordinates - массив точек маршрута
[
    [55.7558, 37.6173], // [lat, lon] - первая точка
    [55.7560, 37.6175], // вторая точка
    [55.7562, 37.6177], // третья точка
    // ... и так далее
]
```

---

## 🔍 Как проверить пересечение маршрута с опасными зонами?

### Алгоритм:

1. **Для каждого маршрута:** получить все точки линии
2. **Для каждой опасной зоны (круга):**
   - Центр: `[centerLat, centerLon]`
   - Радиус: `radiusMeters`
3. **Проверить, проходит ли маршрут через круг:**
   - Вычислить расстояние от каждой точки маршрута до центра круга
   - Если расстояние ≤ радиус → маршрут проходит через зону

### Формула расстояния (Haversine):

```javascript
/**
 * Вычисляет расстояние между двумя точками на Земле (в метрах)
 * @param {number} lat1 - Широта первой точки
 * @param {number} lon1 - Долгота первой точки
 * @param {number} lat2 - Широта второй точки
 * @param {number} lon2 - Долгота второй точки
 * @returns {number} Расстояние в метрах
 */
function distance(lat1, lon1, lat2, lon2) {
    const R = 6371000; // Радиус Земли в метрах
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a = Math.sin(dLat/2) * Math.sin(dLat/2) +
              Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
              Math.sin(dLon/2) * Math.sin(dLon/2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
    return R * c; // Расстояние в метрах
}
```

### Проверка пересечения:

```javascript
/**
 * Проверяет, проходит ли маршрут через опасную зону
 * @param {Array} routeCoordinates - Массив координат маршрута [[lat, lon], ...]
 * @param {Object} zone - Опасная зона {center: [lat, lon], radius: number}
 * @returns {boolean} true если маршрут проходит через зону
 */
function routePassesThroughZone(routeCoordinates, zone) {
    const [zoneLat, zoneLon] = zone.center;
    const zoneRadius = zone.radius;
    
    for (let point of routeCoordinates) {
        const [pointLat, pointLon] = point;
        const dist = distance(pointLat, pointLon, zoneLat, zoneLon);
        
        if (dist <= zoneRadius) {
            return true; // Маршрут проходит через зону
        }
    }
    
    return false; // Маршрут не проходит через зону
}
```

### Оптимизация проверки:

```javascript
/**
 * Оптимизированная проверка с предварительной фильтрацией по bbox
 */
function routePassesThroughZoneOptimized(routeCoordinates, zone) {
    // 1. Быстрая проверка bbox (прямоугольник)
    const routeBbox = getRouteBbox(routeCoordinates);
    if (!bboxesIntersect(routeBbox, zone.bbox)) {
        return false; // Маршрут точно не проходит через зону
    }
    
    // 2. Точная проверка расстояния (только если bbox пересекается)
    const [zoneLat, zoneLon] = zone.center;
    const zoneRadius = zone.radius;
    
    // Проверяем не все точки, а каждую N-ю (для оптимизации)
    const step = Math.max(1, Math.floor(routeCoordinates.length / 100));
    
    for (let i = 0; i < routeCoordinates.length; i += step) {
        const [pointLat, pointLon] = routeCoordinates[i];
        const dist = distance(pointLat, pointLon, zoneLat, zoneLon);
        
        if (dist <= zoneRadius) {
            return true;
        }
    }
    
    return false;
}
```

---

## 📊 Как оценить опасность маршрута?

### Вариант 1: Подсчет пересечений

**Идея:** Подсчитать, через сколько зон проходит маршрут, учитывая уровень опасности.

```javascript
function calculateRouteSafety(routeCoordinates, allZones) {
    let lowCount = 0;
    let mediumCount = 0;
    let highCount = 0;
    const zonesPassed = [];
    
    for (let zone of allZones) {
        if (routePassesThroughZone(routeCoordinates, zone)) {
            zonesPassed.push(zone);
            
            if (zone.risk_level === 'low') lowCount++;
            else if (zone.risk_level === 'medium') mediumCount++;
            else if (zone.risk_level === 'high') highCount++;
        }
    }
    
    // Оценка опасности (баллы)
    const safetyScore = lowCount * 1 + mediumCount * 2 + highCount * 3;
    
    return {
        totalZones: zonesPassed.length,
        lowCount,
        mediumCount,
        highCount,
        safetyScore,
        zones: zonesPassed
    };
}
```

**Плюсы:**
- ✅ Простая реализация
- ✅ Понятная метрика

**Минусы:**
- ⚠️ Не учитывает длину пути через зоны
- ⚠️ Одна точка пересечения = одна зона (даже если маршрут быстро проходит)

### Вариант 2: Длина пути через опасные зоны

**Идея:** Вычислить длину участка маршрута внутри каждой зоны.

```javascript
function calculateRouteSafetyByLength(routeCoordinates, allZones) {
    let totalDangerousLength = 0;
    let lowLength = 0;
    let mediumLength = 0;
    let highLength = 0;
    const zonesPassed = [];
    
    for (let zone of allZones) {
        const lengthInZone = calculateLengthInZone(routeCoordinates, zone);
        
        if (lengthInZone > 0) {
            zonesPassed.push({zone, length: lengthInZone});
            totalDangerousLength += lengthInZone;
            
            if (zone.risk_level === 'low') lowLength += lengthInZone;
            else if (zone.risk_level === 'medium') mediumLength += lengthInZone;
            else if (zone.risk_level === 'high') highLength += lengthInZone;
        }
    }
    
    // Оценка опасности (метры × коэффициент)
    const safetyScore = lowLength * 1 + mediumLength * 2 + highLength * 3;
    
    return {
        totalDangerousLength,
        lowLength,
        mediumLength,
        highLength,
        safetyScore,
        zones: zonesPassed
    };
}

/**
 * Вычисляет длину участка маршрута внутри зоны
 */
function calculateLengthInZone(routeCoordinates, zone) {
    const [zoneLat, zoneLon] = zone.center;
    const zoneRadius = zone.radius;
    let lengthInZone = 0;
    
    for (let i = 0; i < routeCoordinates.length - 1; i++) {
        const [lat1, lon1] = routeCoordinates[i];
        const [lat2, lon2] = routeCoordinates[i + 1];
        
        const dist1 = distance(lat1, lon1, zoneLat, zoneLon);
        const dist2 = distance(lat2, lon2, zoneLat, zoneLon);
        const segmentLength = distance(lat1, lon1, lat2, lon2);
        
        // Если оба конца сегмента внутри зоны
        if (dist1 <= zoneRadius && dist2 <= zoneRadius) {
            lengthInZone += segmentLength;
        }
        // Если один конец внутри, другой снаружи - вычисляем часть внутри
        else if (dist1 <= zoneRadius || dist2 <= zoneRadius) {
            // Упрощенный расчет: половина сегмента
            lengthInZone += segmentLength / 2;
        }
    }
    
    return lengthInZone;
}
```

**Плюсы:**
- ✅ Учитывает длину пути через зоны
- ✅ Более точная оценка

**Минусы:**
- ⚠️ Сложнее реализация
- ⚠️ Больше вычислений

### Вариант 3: Комбинированный подход

**Идея:** Учитывать и количество пересечений, и длину пути.

```javascript
function calculateRouteSafetyCombined(routeCoordinates, allZones) {
    const routeLength = calculateRouteLength(routeCoordinates);
    
    let lowCount = 0, mediumCount = 0, highCount = 0;
    let lowLength = 0, mediumLength = 0, highLength = 0;
    let maxRiskLevel = 'safe';
    const zonesPassed = [];
    
    for (let zone of allZones) {
        const lengthInZone = calculateLengthInZone(routeCoordinates, zone);
        
        if (lengthInZone > 0) {
            zonesPassed.push({zone, length: lengthInZone});
            
            if (zone.risk_level === 'low') {
                lowCount++;
                lowLength += lengthInZone;
            } else if (zone.risk_level === 'medium') {
                mediumCount++;
                mediumLength += lengthInZone;
                if (maxRiskLevel === 'safe' || maxRiskLevel === 'low') {
                    maxRiskLevel = 'medium';
                }
            } else if (zone.risk_level === 'high') {
                highCount++;
                highLength += lengthInZone;
                maxRiskLevel = 'high';
            }
        }
    }
    
    // Комбинированная оценка
    const countScore = lowCount * 1 + mediumCount * 2 + highCount * 3;
    const lengthScore = (lowLength * 1 + mediumLength * 2 + highLength * 3) / 1000; // в км
    const safetyScore = countScore * 0.4 + lengthScore * 0.6; // Веса можно настроить
    
    // Процент опасного пути
    const dangerousPercentage = ((lowLength + mediumLength + highLength) / routeLength) * 100;
    
    return {
        totalZones: zonesPassed.length,
        lowCount, mediumCount, highCount,
        lowLength, mediumLength, highLength,
        maxRiskLevel,
        safetyScore,
        dangerousPercentage,
        zones: zonesPassed
    };
}
```

**Плюсы:**
- ✅ Учитывает все факторы
- ✅ Более точная оценка

**Минусы:**
- ⚠️ Сложнее реализация
- ⚠️ Нужно настраивать веса

---

## 🎨 Что показать пользователю?

### 1. Для каждого из 3 маршрутов:

**Информация:**
- Количество пересечений с опасными зонами
- Список уровней опасности (например: "2 средних, 1 высокий")
- Общая оценка опасности (балл или процент)
- Длина опасного участка (в метрах/километрах)

**Пример отображения:**
```
Маршрут 1:
  ⚠️ Опасных зон: 0
  ✅ Оценка: Безопасный (0 баллов)
  📏 Длина: 12.5 км

Маршрут 2:
  ⚠️ Опасных зон: 3
    - 2 средних (оранжевые)
    - 1 высокий (красный)
  ⚠️ Оценка: Умеренно опасный (7 баллов)
  📏 Длина: 11.8 км
  🔴 Опасный участок: 2.3 км (19%)

Маршрут 3:
  ⚠️ Опасных зон: 5
    - 1 низкий (желтый)
    - 3 средних (оранжевые)
    - 1 высокий (красный)
  🔴 Оценка: Опасный (12 баллов)
  📏 Длина: 10.2 км
  🔴 Опасный участок: 4.1 км (40%)
```

### 2. Визуализация:

**На карте:**
- Подсветить участки маршрута, проходящие через опасные зоны
- Показать зоны, через которые проходит маршрут
- Разные цвета для разных уровней опасности

**Пример:**
```
Маршрут 1: [════════════════] (зеленый - безопасный)
Маршрут 2: [═══🟠═══🔴═══] (оранжевый/красный - опасные участки)
Маршрут 3: [🟡🟠🟠🟠🔴] (много опасных участков)
```

### 3. Рекомендация:

**Автоматическая рекомендация:**
- "Рекомендуем Маршрут 1 - самый безопасный"
- "Маршрут 2 - компромисс между безопасностью и длиной"
- "Маршрут 3 не рекомендуется - проходит через много опасных зон"

**Критерии:**
- Минимальный safetyScore
- Минимальный процент опасного пути
- Минимальное количество зон высокого уровня

---

## 🔧 Технические детали реализации

### Получение маршрутов из routePanelControl:

```javascript
// Инициализация
const routePanel = map.controls.get('routePanelControl');

// Получение маршрутов после построения
routePanel.routePanel.state.events.add('change', function() {
    const routes = routePanel.routePanel.getRoutes();
    
    if (routes && routes.length > 0) {
        routes.forEach(function(route, index) {
            route.getGeometryAsync().then(function(geometry) {
                const coordinates = geometry.getCoordinates();
                analyzeRouteSafety(coordinates, index);
            });
        });
    }
});
```

### Получение маршрутов из MultiRoute:

```javascript
// Создание MultiRoute
routeMultiRoute = new ymaps.multiRouter.MultiRoute({
    referencePoints: [
        [fromLat, fromLon],
        [toLat, toLon]
    ],
    params: {
        routingMode: 'auto'
    }
}, {
    boundsAutoApply: true
});

map.geoObjects.add(routeMultiRoute);

// Получение маршрутов после построения
routeMultiRoute.model.events.add('requestsuccess', function() {
    const routes = routeMultiRoute.getRoutes();
    
    routes.each(function(route, index) {
        const geometry = route.getGeometry();
        const coordinates = geometry.getCoordinates();
        analyzeRouteSafety(coordinates, index);
    });
});
```

### Структура данных опасных зон:

```javascript
// У нас уже есть hotspotsData - массив зон
const zone = {
    geometry: {
        coordinates: [lon, lat] // Центр зоны
    },
    properties: {
        radius: 500, // Радиус в метрах
        risk_level: 'medium', // 'low', 'medium', 'high'
        count: 15, // Количество ДТП
        density_per_1000m2: 0.25
    }
};
```

---

## ⚡ Оптимизация производительности

### 1. Предварительная фильтрация зон

**Идея:** Проверять только зоны, которые могут пересекаться с маршрутом.

```javascript
function getZonesNearRoute(routeCoordinates, allZones) {
    const routeBbox = getRouteBbox(routeCoordinates);
    
    return allZones.filter(zone => {
        // Быстрая проверка bbox
        return bboxesIntersect(routeBbox, zone.bbox);
    });
}

function getRouteBbox(coordinates) {
    let minLat = Infinity, maxLat = -Infinity;
    let minLon = Infinity, maxLon = -Infinity;
    
    for (let [lat, lon] of coordinates) {
        minLat = Math.min(minLat, lat);
        maxLat = Math.max(maxLat, lat);
        minLon = Math.min(minLon, lon);
        maxLon = Math.max(maxLon, lon);
    }
    
    return {minLat, maxLat, minLon, maxLon};
}
```

### 2. Упрощение геометрии маршрута

**Идея:** Не проверять все точки маршрута, а использовать упрощенную версию.

```javascript
function simplifyRoute(coordinates, tolerance) {
    // Использовать алгоритм Douglas-Peucker или просто брать каждую N-ю точку
    const step = Math.max(1, Math.floor(coordinates.length / 100));
    return coordinates.filter((_, i) => i % step === 0);
}
```

### 3. Кэширование расстояний

**Идея:** Кэшировать расстояния между точками маршрута.

```javascript
const distanceCache = new Map();

function getCachedDistance(lat1, lon1, lat2, lon2) {
    const key = `${lat1},${lon1},${lat2},${lon2}`;
    if (!distanceCache.has(key)) {
        distanceCache.set(key, distance(lat1, lon1, lat2, lon2));
    }
    return distanceCache.get(key);
}
```

---

## 📋 План реализации

### Этап 1: Получение маршрутов
- [ ] Подключиться к событиям routePanelControl или MultiRoute
- [ ] Получить координаты всех 3 маршрутов
- [ ] Сохранить маршруты в переменные

### Этап 2: Проверка пересечений
- [ ] Реализовать функцию distance() (Haversine)
- [ ] Реализовать функцию routePassesThroughZone()
- [ ] Оптимизировать проверку (bbox фильтрация)

### Этап 3: Оценка опасности
- [ ] Выбрать алгоритм оценки (Вариант 1, 2 или 3)
- [ ] Реализовать calculateRouteSafety()
- [ ] Протестировать на разных маршрутах

### Этап 4: Визуализация
- [ ] Создать UI для отображения результатов
- [ ] Подсветить опасные участки на карте
- [ ] Показать сравнение маршрутов

### Этап 5: Рекомендации
- [ ] Реализовать логику выбора лучшего маршрута
- [ ] Показать рекомендацию пользователю

---

## 🎯 Пример использования

```javascript
// После построения маршрута
function analyzeAllRoutes() {
    const routes = getRoutesFromYandex(); // Получаем 3 маршрута
    const allZones = hotspotsData; // Все опасные зоны
    
    const results = routes.map((route, index) => {
        const coordinates = route.getCoordinates();
        const safety = calculateRouteSafetyCombined(coordinates, allZones);
        
        return {
            routeIndex: index + 1,
            routeLength: calculateRouteLength(coordinates),
            safety: safety,
            recommendation: getRecommendation(safety)
        };
    });
    
    // Сортируем по безопасности
    results.sort((a, b) => a.safety.safetyScore - b.safety.safetyScore);
    
    // Показываем пользователю
    displayRouteComparison(results);
}
```

---

## 💡 Дополнительные идеи

### 1. Временная оценка

Учитывать время проезда через опасные зоны:
- Днем vs ночью
- В будни vs выходные
- В зависимости от времени суток

### 2. Альтернативные маршруты

Если все маршруты опасные, предложить альтернативу:
- Объехать опасные зоны
- Использовать другой тип маршрута (пешком, общественный транспорт)

### 3. История опасности

Сохранять историю анализа маршрутов:
- Какие маршруты пользователь выбирал
- Статистика по опасности выбранных маршрутов

### 4. Уведомления

Предупреждать пользователя:
- "Впереди опасная зона через 500м"
- "Рекомендуем снизить скорость"

---

## 📊 Метрики для оценки

### Базовые метрики:
- **Количество зон** - сколько опасных зон пересекает маршрут
- **Балл опасности** - взвешенная сумма (low=1, medium=2, high=3)
- **Длина опасного участка** - сколько метров/километров через опасные зоны
- **Процент опасного пути** - доля опасного участка от общей длины

### Дополнительные метрики:
- **Максимальный уровень опасности** - самый опасный уровень на маршруте
- **Плотность опасных зон** - количество зон на километр пути
- **Средняя плотность ДТП** - средняя плотность всех пересеченных зон

---

## 🔍 Сложность реализации

### Простая версия (Вариант 1):
- **Время:** 2-4 часа
- **Сложность:** ⭐⭐
- **Функции:** Подсчет пересечений, базовая оценка

### Средняя версия (Вариант 2):
- **Время:** 4-6 часов
- **Сложность:** ⭐⭐⭐
- **Функции:** Расчет длины в зонах, оптимизация

### Полная версия (Вариант 3):
- **Время:** 6-8 часов
- **Сложность:** ⭐⭐⭐⭐
- **Функции:** Комбинированная оценка, визуализация, рекомендации

---

## ✅ Выводы

1. **Технически реализуемо** - Yandex.Maps API предоставляет доступ к координатам маршрутов
2. **Алгоритм понятен** - проверка расстояния от точек маршрута до центров зон
3. **Оценка опасности** - можно использовать разные подходы (подсчет, длина, комбинация)
4. **Визуализация** - можно подсветить опасные участки на карте
5. **Полезно для пользователя** - помогает выбрать безопасный маршрут

---

**Дата создания:** 2026-01-22  
**Статус:** Концепция, готово к реализации  
**Приоритет:** Средний





