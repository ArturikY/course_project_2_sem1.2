# 📐 Что такое размер ячейки сетки (gridSize) и почему круги накладываются?

## ❓ Что такое gridSize?

**gridSize** = размер стороны прямоугольной ячейки сетки в метрах.

### Как это работает:

```
gridSize = 1000м означает:
┌─────────┐
│         │ ← 1000 метров
│  Ячейка │
│         │
└─────────┘
    ↑
 1000 метров
```

**Каждая ячейка** = квадрат со стороной `gridSize` метров.

---

## ⭕ Почему круги накладываются?

### Текущая формула радиуса:

```php
$radiusMeters = $gridSize / 2;
```

**Это означает:**
- `gridSize = 1000м` → `radius = 500м`
- `gridSize = 2000м` → `radius = 1000м`
- `gridSize = 3000м` → `radius = 1500м`

### Расстояние между центрами кругов:

**Центры кругов** находятся в центрах ячеек, поэтому расстояние между ними = `gridSize`.

```
gridSize = 1000м:
  ○───────○───────○
  │  500м │  500м │
  │  ←───→│←───→  │
  │       │       │
  ○───────○───────○
  
Расстояние между центрами = 1000м
Радиус каждого круга = 500м
```

### Что происходит:

**1. gridSize = 1000м:**
```
Расстояние между центрами: 1000м
Радиус круга: 500м
Результат: Круги касаются друг друга (500 + 500 = 1000) ✓
```

**2. gridSize = 2000м:**
```
Расстояние между центрами: 2000м
Радиус круга: 1000м
Результат: Круги касаются друг друга (1000 + 1000 = 2000) ✓
```

**3. gridSize = 3000м:**
```
Расстояние между центрами: 3000м
Радиус круга: 1500м
Результат: Круги касаются друг друга (1500 + 1500 = 3000) ✓
```

**4. gridSize = 500м:**
```
Расстояние между центрами: 500м
Радиус круга: 250м
Результат: Круги касаются друг друга (250 + 250 = 500) ✓
```

---

## 🤔 Почему это происходит?

### Логика текущего алгоритма:

**Идея:** Круг должен **покрывать** ячейку, поэтому радиус = половина размера ячейки.

```
┌─────────┐
│    ○    │ ← Круг покрывает ячейку
│   ╱ ╲   │
│  ╱   ╲  │
│ ╱     ╲ │
└─────────┘
```

**Проблема:** Если ячейки касаются друг друга, и круги покрывают ячейки, то круги **обязательно** будут касаться или накладываться!

---

## 📊 Визуализация проблемы

### gridSize = 1000м:

```
Ячейки:          Круги:
┌─────┬─────┐    ╭─────╮╭─────╮
│  A  │  B  │ →  │  ○  ││  ○  │  ← Круги касаются
└─────┴─────┘    ╰─────╯╰─────╯
     ↑                ↑
  Центры          Центры
```

### gridSize = 500м (меньше):

```
Ячейки:              Круги:
┌───┬───┬───┬───┐    ╭───╮╭───╮╭───╮╭───╮
│ A │ B │ C │ D │ →  │ ○ ││ ○ ││ ○ ││ ○ │  ← Круги касаются
└───┴───┴───┴───┘    ╰───╯╰───╯╰───╯╰───╯
```

### gridSize = 2000м (больше):

```
Ячейки:        Круги:
┌───────┐      ╭───────╮
│   A   │  →   │   ○   │  ← Круги касаются
└───────┘      ╰───────╯
```

**Вывод:** При любой gridSize круги будут касаться друг друга, потому что `radius = gridSize / 2`.

---

## ⚠️ Когда круги накладываются?

Круги накладываются, если:

1. **Радиус > gridSize / 2** (но у нас всегда = gridSize / 2)
2. **Ячейки перекрываются** (но они не перекрываются)
3. **ДТП попадает в несколько ячеек** (но алгоритм распределяет каждое ДТП только в одну ячейку)

**В текущем алгоритме круги НЕ должны накладываться**, они должны только касаться.

---

## 🔍 Почему кажется, что они накладываются?

### Возможные причины:

1. **Визуальное восприятие:**
   - Круги с прозрачностью (`fillOpacity: 0.3`)
   - При наложении краев создается эффект "наложения"
   - На самом деле они только касаются

2. **Несколько зон рядом:**
   - Если в соседних ячейках есть зоны
   - Они касаются друг друга
   - Визуально выглядит как наложение

3. **Разные уровни опасности:**
   - Оранжевые и красные круги рядом
   - Разные цвета создают эффект наложения

---

## 💡 Как исправить (идеи)

### Вариант 1: Фиксированный радиус (независимо от gridSize)

**Идея:** Использовать один и тот же радиус для всех зон.

```php
$radiusMeters = 400; // Фиксированный радиус (не зависит от gridSize)
```

**Результат:**
- ✅ Все круги одинакового размера
- ✅ При gridSize > 800м круги не будут касаться
- ✅ При gridSize < 800м круги могут перекрываться (но это нормально)

**Пример:**
```
gridSize = 1000м, radius = 400м:
  ○───────○
  │ 400м  │  ← Расстояние 1000м, радиус 400м
  │       │  ← Круги НЕ касаются (зазор 200м)
  
gridSize = 500м, radius = 400м:
  ○───○
  │400│  ← Расстояние 500м, радиус 400м
  │   │  ← Круги перекрываются (но это нормально)
```

### Вариант 2: Радиус = gridSize / 3 (вместо / 2)

**Идея:** Уменьшить радиус, чтобы круги не касались.

```php
$radiusMeters = $gridSize / 3; // Вместо / 2
```

**Результат:**
- ✅ Круги не касаются (зазор между ними)
- ✅ Но могут не покрывать всю ячейку

**Пример:**
```
gridSize = 1000м, radius = 333м:
  ○───────○
  │ 333м  │  ← Расстояние 1000м, радиус 333м
  │       │  ← Зазор 334м между кругами
```

### Вариант 3: Адаптивный радиус

**Идея:** Радиус зависит от количества ДТП, но с ограничением.

```php
$baseRadius = min($gridSize / 3, 500); // Максимум 500м
$radiusMeters = $baseRadius + min($count * 5, 200); // + до 200м за ДТП
```

**Результат:**
- ✅ Круги не накладываются (базовый радиус < gridSize / 2)
- ✅ Больше ДТП → немного больший круг
- ✅ Но все еще ограничено

---

## 📐 Математика

### Текущая формула:

```
radius = gridSize / 2
distance_between_centers = gridSize

gap = distance_between_centers - (radius1 + radius2)
gap = gridSize - (gridSize/2 + gridSize/2)
gap = gridSize - gridSize
gap = 0  ← Круги касаются (зазор = 0)
```

### С фиксированным радиусом:

```
radius = 400м (фиксированный)
distance_between_centers = gridSize

gap = gridSize - (400 + 400)
gap = gridSize - 800

Если gridSize = 1000м: gap = 200м (не касаются) ✓
Если gridSize = 500м: gap = -300м (перекрываются) ⚠️
```

### С radius = gridSize / 3:

```
radius = gridSize / 3
distance_between_centers = gridSize

gap = gridSize - (gridSize/3 + gridSize/3)
gap = gridSize - 2*gridSize/3
gap = gridSize / 3  ← Всегда есть зазор
```

---

## 🎯 Рекомендации

### Для предотвращения наложения:

1. **Использовать фиксированный радиус** (Вариант 1)
   - Рекомендуемый радиус: 300-500м
   - При gridSize > 1000м круги не будут касаться

2. **Использовать radius = gridSize / 3** (Вариант 2)
   - Гарантирует зазор между кругами
   - Но круги могут не покрывать всю ячейку

3. **Комбинированный подход:**
   ```php
   // Минимальный радиус для покрытия ячейки
   $minRadius = $gridSize / 2;
   
   // Максимальный радиус (чтобы не накладывались)
   $maxRadius = $gridSize / 2.5;
   
   // Используем меньший из двух
   $radiusMeters = min($minRadius, $maxRadius);
   ```

---

## 📊 Сравнение

| gridSize | Текущий (radius = gridSize/2) | Фиксированный (400м) | radius = gridSize/3 |
|----------|-------------------------------|----------------------|---------------------|
| 500м     | Касаются (250+250=500)        | Перекрываются        | Зазор 167м          |
| 1000м    | Касаются (500+500=1000)       | Зазор 200м ✓         | Зазор 333м ✓        |
| 2000м    | Касаются (1000+1000=2000)     | Зазор 1200м ✓        | Зазор 667м ✓        |
| 3000м    | Касаются (1500+1500=3000)     | Зазор 2200м ✓        | Зазор 1000м ✓       |

---

## 🎯 Выводы

1. **Текущий алгоритм:** `radius = gridSize / 2` → круги всегда касаются друг друга
2. **Это нормально** для покрытия ячеек, но может выглядеть как наложение
3. **Для предотвращения касания:** использовать фиксированный радиус или `gridSize / 3`
4. **Размер сетки (gridSize)** = размер стороны ячейки, не размер круга!

---

**Дата:** 2026-01-22  
**Версия:** 1.0

