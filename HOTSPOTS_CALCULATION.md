# 📐 Алгоритм расчета опасных зон (кругов)

## 🎯 Общая концепция

Опасные зоны представляют собой **круги** на карте, которые показывают участки с высокой плотностью ДТП. Каждая зона имеет:

- **Центр** (координаты)
- **Радиус** (в метрах)
- **Уровень опасности** (medium или high)
- **Плотность ДТП** (коэффициент на 1000 м²)

---

## 📊 Шаг 1: Создание сетки ячеек

### Параметры:

- `bbox` - границы области (minLon, minLat, maxLon, maxLat)
- `gridSize` - размер ячейки в метрах (по умолчанию 1000м)

### Расчет шага сетки:

```php
// Шаг по широте (в градусах)
$latStep = $gridSize / 111000;  // 1° ≈ 111 км

// Шаг по долготе (зависит от широты)
$centerLat = ($minLat + $maxLat) / 2;
$lonStep = $gridSize / (111000 * cos(deg2rad($centerLat)));
```

**Пример для Москвы (gridSize=1000м):**

- `latStep = 1000 / 111000 ≈ 0.009°` (≈ 1000 метров)
- `lonStep = 1000 / (111000 * cos(55.75°)) ≈ 0.0156°` (≈ 1000 метров)

### Создание ячеек:

```php
$latCells = ceil(($maxLat - $minLat) / $latStep);
$lonCells = ceil(($maxLon - $minLon) / $lonStep);

// Создаем двумерный массив ячеек
for ($i = 0; $i < $latCells; $i++) {
    for ($j = 0; $j < $lonCells; $j++) {
        $cellMinLat = $minLat + $i * $latStep;
        $cellMaxLat = min($cellMinLat + $latStep, $maxLat);
        $cellMinLon = $minLon + $j * $lonStep;
        $cellMaxLon = min($cellMinLon + $lonStep, $maxLon);

        $cells[$i][$j] = [
            'minLat' => $cellMinLat,
            'maxLat' => $cellMaxLat,
            'minLon' => $cellMinLon,
            'maxLon' => $cellMaxLon,
            'count' => 0,
            'severeCount' => 0
        ];
    }
}
```

**Результат:** Создается сетка прямоугольных ячеек, покрывающая всю область bbox.

---

## 📍 Шаг 2: Распределение ДТП по ячейкам

### Для каждого ДТП:

1. **Проверка bbox:**

   ```php
   if ($lat < $minLat || $lat > $maxLat ||
       $lon < $minLon || $lon > $maxLon) {
       continue; // Пропускаем ДТП вне области
   }
   ```

2. **Фильтр по дате** (если указан период):

   ```php
   if ($dateFrom && $item['dt']) {
       $dtParsed = strtotime($item['dt']);
       if ($dtParsed < $dateFrom) {
           continue; // Пропускаем старые ДТП
       }
   }
   ```

3. **Вычисление индекса ячейки** (O(1) доступ):

   ```php
   $latIndex = floor(($lat - $minLat) / $latStep);
   $lonIndex = floor(($lon - $minLon) / $lonStep);
   ```

4. **Увеличение счетчика:**
   ```php
   $cells[$latIndex][$lonIndex]['count']++;
   if ($severity === 'Тяжелый' || $severity === 'Смертельный') {
       $cells[$latIndex][$lonIndex]['severeCount']++;
   }
   ```

**Результат:** Каждая ячейка содержит количество ДТП в ней.

---

## 🔍 Шаг 3: Фильтрация по порогу

### Порог (threshold):

- По умолчанию: `threshold = 5`
- Ячейки с количеством ДТП < threshold **не учитываются**

```php
foreach ($cells as $cell) {
    if ($cell['count'] >= $threshold) {
        $result[] = $cell; // Добавляем в результат
    }
}
```

**Результат:** Остаются только ячейки с достаточным количеством ДТП.

---

## ⭕ Шаг 4: Преобразование ячеек в круги

### Для каждой ячейки:

1. **Расчет центра:**

   ```php
   $centerLat = ($cell['minLat'] + $cell['maxLat']) / 2;
   $centerLon = ($cell['minLon'] + $cell['maxLon']) / 2;
   ```

2. **Расчет радиуса:**

   ```php
   $radiusMeters = $gridSize / 2;  // Половина размера ячейки
   ```

   **Пример:** При `gridSize=1000м` → `radius=500м`

3. **Расчет площади:**

   ```php
   $areaMeters = M_PI * $radiusMeters * $radiusMeters;
   ```

   **Пример:** При `radius=500м` → `area ≈ 785,398 м²`

4. **Расчет плотности:**

   ```php
   $densityPer1000m2 = ($count / $areaMeters) * 1000;
   ```

   **Пример:** При `count=20`, `area=785,398 м²`:
   - `densityPer1000m2 = (20 / 785,398) * 1000 ≈ 0.0255`

---

## 🎨 Шаг 5: Определение уровня опасности

### Пороги плотности:

```php
if ($densityPer1000m2 >= 0.3) {
    $riskLevel = 'high';      // Красный
} elseif ($densityPer1000m2 >= 0.2) {
    $riskLevel = 'medium';    // Оранжевый
} else {
    // Пропускаем (не отображаем)
    continue;
}
```

**Текущие пороги:**

- **Medium (оранжевый):** 0.2 - 0.3 на 1000 м²
- **High (красный):** ≥ 0.3 на 1000 м²
- **Low:** < 0.2 (не отображается)

---

## 📐 Формулы в деталях

### 1. Размер ячейки → Радиус круга

```
radius = gridSize / 2
```

**Логика:** Круг должен покрывать ячейку, поэтому радиус = половина размера ячейки.

**Примеры:**

- `gridSize = 250м` → `radius = 125м`
- `gridSize = 1000м` → `radius = 500м`
- `gridSize = 2000м` → `radius = 1000м`

### 2. Площадь круга

```
area = π × r²
```

**Примеры:**

- `r = 125м` → `area ≈ 49,087 м²`
- `r = 500м` → `area ≈ 785,398 м²`
- `r = 1000м` → `area ≈ 3,141,593 м²`

### 3. Плотность ДТП

```
density = (count / area) × 1000
```

**Единица измерения:** Количество ДТП на 1000 м²

**Примеры:**

- `count = 10`, `area = 49,087 м²` → `density = 0.204`
- `count = 20`, `area = 785,398 м²` → `density = 0.0255`
- `count = 100`, `area = 785,398 м²` → `density = 0.127`

---

## ⚠️ Проблемы текущего алгоритма

### 1. **Нестабильность при изменении gridSize**

**Проблема:**

- При `gridSize=250м` → `radius=125м`, `area≈49,087 м²`
- При `gridSize=1000м` → `radius=500м`, `area≈785,398 м²`

**Результат:**

- Одна и та же зона может иметь разную плотность при разном gridSize
- Круги могут "прыгать" при изменении размера сетки

### 2. **Граничные эффекты**

**Проблема:**

- ДТП на границе ячейки может попасть в соседнюю ячейку
- При изменении bbox (движении карты) ячейки пересчитываются
- Круги могут появляться/исчезать при движении карты

### 3. **Зависимость от размера ячейки**

**Проблема:**

- Большие ячейки → большие круги → меньше плотность
- Маленькие ячейки → маленькие круги → больше плотность
- Один и тот же участок может иметь разный уровень опасности

---

## 💡 Идеи для стабилизации кругов

### Вариант 1: Фиксированный радиус

**Идея:** Использовать фиксированный радиус для всех зон, независимо от gridSize.

```php
$radiusMeters = 500; // Фиксированный радиус
$areaMeters = M_PI * $radiusMeters * $radiusMeters;
```

**Плюсы:**

- ✅ Круги одинакового размера
- ✅ Плотность не зависит от gridSize
- ✅ Более предсказуемое поведение

**Минусы:**

- ⚠️ Круги могут не покрывать ячейки полностью
- ⚠️ Нужно выбирать оптимальный радиус

### Вариант 2: Адаптивный радиус по количеству ДТП

**Идея:** Радиус зависит от количества ДТП в зоне.

```php
// Базовый радиус
$baseRadius = 300;

// Увеличиваем радиус при большом количестве ДТП
$radiusMeters = $baseRadius + min($count * 10, 500);
```

**Плюсы:**

- ✅ Больше ДТП → больше зона
- ✅ Визуально понятнее

**Минусы:**

- ⚠️ Плотность меняется с радиусом
- ⚠️ Сложнее сравнивать зоны

### Вариант 3: Использование KDE (Kernel Density Estimation)

**Идея:** Использовать статистический метод для расчета плотности.

```php
// Для каждой точки ДТП создаем "ядро" (например, гауссово)
// Плотность = сумма всех ядер в данной точке
```

**Плюсы:**

- ✅ Более точная оценка плотности
- ✅ Плавные переходы между зонами
- ✅ Не зависит от размера сетки

**Минусы:**

- ⚠️ Сложнее реализация
- ⚠️ Больше вычислений

### Вариант 4: Кластеризация DBSCAN

**Идея:** Использовать алгоритм кластеризации для группировки ДТП.

```php
// Группируем близкие ДТП в кластеры
// Радиус круга = радиус кластера + буфер
```

**Плюсы:**

- ✅ Автоматическое определение размера зон
- ✅ Учитывает реальное распределение ДТП

**Минусы:**

- ⚠️ Сложная реализация
- ⚠️ Нужно настраивать параметры

### Вариант 5: Фиксированная площадь для расчета плотности

**Идея:** Всегда использовать одну и ту же площадь для расчета плотности.

```php
$standardArea = 100000; // 100,000 м² = 0.1 км²
$densityPer1000m2 = ($count / $standardArea) * 1000;
```

**Плюсы:**

- ✅ Плотность не зависит от размера круга
- ✅ Легко сравнивать зоны

**Минусы:**

- ⚠️ Радиус круга все еще зависит от gridSize
- ⚠️ Может быть неинтуитивно

---

## 🔧 Рекомендации

### Для стабильности:

1. **Использовать фиксированный радиус** (Вариант 1)
   - Проще всего реализовать
   - Предсказуемое поведение
   - Рекомендуемый радиус: 300-500 метров

2. **Использовать фиксированную площадь для плотности** (Вариант 5)
   - Плотность не зависит от размера круга
   - Легко сравнивать зоны
   - Рекомендуемая площадь: 100,000 м² (0.1 км²)

3. **Комбинированный подход:**

   ```php
   // Фиксированный радиус для отображения
   $radiusMeters = 400; // Все круги одинакового размера

   // Фиксированная площадь для расчета плотности
   $standardArea = 100000; // 0.1 км²
   $densityPer1000m2 = ($count / $standardArea) * 1000;
   ```

### Для точности:

1. **KDE** (Вариант 3) - самый точный, но сложный
2. **DBSCAN** (Вариант 4) - хороший баланс точности и сложности

---

## 📊 Примеры расчетов

### Пример 1: gridSize=1000м, count=20

```
radius = 1000 / 2 = 500м
area = π × 500² ≈ 785,398 м²
density = (20 / 785,398) × 1000 ≈ 0.0255
riskLevel = null (пропускаем, т.к. < 0.2)
```

### Пример 2: gridSize=1000м, count=200

```
radius = 500м
area = 785,398 м²
density = (200 / 785,398) × 1000 ≈ 0.255
riskLevel = 'medium' (0.2 ≤ 0.255 < 0.3)
```

### Пример 3: gridSize=1000м, count=300

```
radius = 500м
area = 785,398 м²
density = (300 / 785,398) × 1000 ≈ 0.382
riskLevel = 'high' (≥ 0.3)
```

### Пример 4: gridSize=250м, count=20

```
radius = 250 / 2 = 125м
area = π × 125² ≈ 49,087 м²
density = (20 / 49,087) × 1000 ≈ 0.407
riskLevel = 'high' (≥ 0.3)
```

**Вывод:** Одно и то же количество ДТП (20) дает разный уровень опасности в зависимости от gridSize!

---

## 🎯 Выводы

1. **Текущий алгоритм нестабилен** - зависит от gridSize
2. **Для стабильности нужно:**
   - Фиксированный радиус кругов
   - Фиксированная площадь для расчета плотности
3. **Для точности можно использовать:**
   - KDE (Kernel Density Estimation)
   - DBSCAN кластеризацию

---

**Дата создания:** 2026-01-22  
**Версия:** 1.0




